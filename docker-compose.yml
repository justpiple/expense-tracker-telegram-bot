services:
  bot:
    build: .
    container_name: expense-tracker-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      # Persistent storage for data folder (users.json, etc.)
      - expense-data:/app/data

volumes:
  expense-data:
